apiVersion: v1
kind: Service
metadata:
  name: {{ include "youssef-v1.fullname" . }}-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "youssef-v1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    {{- include "youssef-v1.labels" . | nindent 4 }}
